schema_version: 1
tempo_bpm: 120
planner_lookahead_ms: 20

# Reference bindings implemented by the web arranger. Loading this file into
# the runtime remains a later configuration milestone.
controls:
  intro: i
  start: enter
  stop: space
  ending: e
  left_hand_sync: s
